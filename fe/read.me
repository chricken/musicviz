Neuer Plan:

Da das direkte auslesen des Spektrums aus einem Audiofile ziemlich schwierig ist, werdeich nun zunächst das Audiofile einmal durchlaufen lassen und die Spektren in ein großes Array schreiben.
Dieses kann bspw. ein JSON gespeichert werden.
Die Visualisierung kann dann über das Array geschehen. Damit ist die Abhängigkeit vom Audio-Timing aufgehoben.